[project]
name = "dgpredict-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9",
    "asyncpg>=0.29",
    "python-dotenv>=1.0",
    "pendulum>=3.0",
]
